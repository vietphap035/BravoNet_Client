<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BravoNet_Client.Dathang"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BravoNet_Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:BravoNet_Client.Model"
    mc:Ignorable="d"
    Title="Dathang">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="10">
        <StackPanel>
            <!-- Header -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Ellipse Width="50" Height="50" Fill="Gray"/>
                <TextBlock Text="Nguyen Viet Phap" FontSize="18" VerticalAlignment="Center" Margin="10,0"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
                    <Button Content="Hủy" HorizontalAlignment="Right" Click="Huy_btn"/>
                    <Button Content="Xác nhận" HorizontalAlignment="Right" Click="XacNhan_btn"/>
                </StackPanel>
            </StackPanel>

            <!-- Danh sách sản phẩm -->
            <ListView ItemsSource="{x:Bind ProductList, Mode=OneWay}" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="model:SanPhamHoaDon">
                        <Border Background="White" CornerRadius="8" Margin="0,5" Padding="10">
                            <Grid ColumnDefinitions="Auto,*,Auto" VerticalAlignment="Center">
                                <!-- Ảnh sản phẩm -->
                                <Image Source="{Binding img}" Width="60" Height="60" Stretch="UniformToFill" />

                                <!-- Thông tin sản phẩm -->
                                <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind name}" FontSize="16" FontWeight="SemiBold"/>
                                    <TextBlock Text="{x:Bind price_at_order, Mode=OneWay}" FontSize="14" Foreground="DarkGreen"/>
                                </StackPanel>

                                <!-- Số lượng -->
                                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Spacing="5">
                                    <Button Content="-" Click="Decrease_Click"/>
                                    <TextBlock Text="{x:Bind quantity, Mode=OneWay}" FontSize="14" VerticalAlignment="Center"/>
                                    <Button Content="+" Click="Increase_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Window>
