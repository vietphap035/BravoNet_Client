<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BravoNet_Client.Hoadon"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BravoNet_Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid>
        <controls:DataGrid x:Name="DataGridHoaDon"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  Margin="10"
                  ItemsSource="{x:Bind MyDataList, Mode=OneWay}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="STT" Binding="{Binding STT}" Width="*"/>
                <controls:DataGridTextColumn Header="Mã hóa đơn"
                              Binding="{Binding OrderId}"
                              Visibility="Collapsed" />
                <controls:DataGridTextColumn Header="Tổng tiền" Binding="{Binding Tong_tien}" Width="*"/>
                <controls:DataGridTemplateColumn Header="Chi tiết" Width="Auto">
                    <controls:DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Xem chi tiết"
                                    Click="Detail_Btn_Click"
                                    Tag="{Binding}"
                                    Padding="6,2"/>
                        </DataTemplate>
                    </controls:DataGridTemplateColumn.CellTemplate>
                </controls:DataGridTemplateColumn>
            </controls:DataGrid.Columns>
        </controls:DataGrid>

        <Button x:Name="AddButton"
                Content="+"
                Width="50"
                Height="50"
                FontSize="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Margin="0,0,0,10"
                Click="AddButton_Click"
                Style="{StaticResource AccentButtonStyle}"
                CornerRadius="20"/>
    </Grid>
</Page>
